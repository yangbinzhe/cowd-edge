# WeCom Message Connector

Builds to `cowd-edge-wecom-message`. It owns WeCom callbacks, message crypto, and
message delivery.

Gateway only forwards callback HTTP traffic and dispatches JSONL frames.
